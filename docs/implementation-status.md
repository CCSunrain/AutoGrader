# 实现状态跟踪（Implementation Status）

> 更新：2026-09-16
> 约定：模块状态在 **待做 → 进行中 → 已实现 → 已验证 → 已发布** 之间推进。
> - **待做**：尚未开始
> - **进行中**：已动工但未完成
> - **已实现**：功能代码完成，通过本地自测
> - **已验证**：通过自动化测试 / 真实数据核验
> - **已发布**：已部署到在线环境并可通过链接访问
>
> 状态变更时更新本表，并在「阻塞/依赖」列注明未决项（对应 `project-baseline.md` §8）。

## 一、基础设施与租户

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 1 | 登录认证（注册/登录/会话） | 已验证 | 冒烟：注册/登录/me 通过 |
| 2 | 多租户数据隔离（tenant_id） | 已验证 | 冒烟：跨工作区隔离通过 |
| 3 | 角色权限模型（owner/teacher/ta/student/demo） | 已实现 | deps 就位；跨角色端到端待补 |
| 4 | demo 临时工作区（可重置） | 待做 | — |
| 5 | PostgreSQL 与 Alembic 迁移骨架 | 已实现 | alembic 配置 + init_db 就位；PG 实跑待 Docker 环境 |

## 二、教学业务

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 6 | 课程管理 | 已验证 | 冒烟：CRUD + 隔离通过 |
| 7 | 作业管理 | 已实现 | create/list 就位，未纳入冒烟 |
| 8 | 评分量表（Rubric）管理（逐项 + 档位） | 已验证 | 冒烟：嵌套 items/bands 通过 |
| 9 | 量表/评分标准版本冻结 | 已验证 | 冒烟：freeze 通过 |

## 三、上传与解析

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 10 | PDF 上传与解析（原文 + 位置 + 表格） | 已验证 | PyMuPDF 文本块/表格/图片 + 页号/bbox/offset；单测 + 端到端通过 |
| 11 | Markdown 上传与解析 | 已验证 | 标题/代码块/表格/列表/段落 + 行号 + offset；单测 + 端到端通过 |
| 12 | 解析产物版本化 | 已验证 | ParsedDocument 绑定 submission_version_id + parser_version |

## 四、循证评阅（核心）

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 13 | 评阅有限状态机（FSM） | 已验证 | Review pending→running→done；端到端通过 |
| 14 | 逐项证据定位（原文高亮） | 已验证 | quote→offset 定位 + page 反查；quote 与原文一致 |
| 15 | supported/missing/uncertain 判定 | 已验证 | 三项状态由 LLM 判定并经合法性校验 |
| 16 | 数值/表格矛盾检测 | 已验证 | 确定性规则：规模递增但耗时反向下降 |
| 17 | 待复核标记（证据不足/解析不全/矛盾/冲突） | 已验证 | uncertain/JSON 失败/档位非法→needs_review，不自动判零 |
| 18 | 结构化输出 + 校验重试 | 已验证 | JSON 提取兜底 + 解析失败重试一次 |
| 19 | 建议档位取自量表（不决定总分） | 已验证 | suggested_band 必须在量表档位内，否则待复核 |
| 20 | 持久任务 + 断点恢复 | 已验证 | 模型 + worker + enqueue + FOR UPDATE SKIP LOCKED 已在 PG 实跑 |

## 五、评阅与发布

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 21 | 三栏评阅台（评分项/原文/建议+决定） | 已实现 | 前端 React+TS 完成（证据高亮/档位选择/发布）；类型检查+转译+proxy 通过，浏览器实测待做 |
| 22 | 人工逐项复核（教师/助教） | 已验证 | POST /review-items/{id}/decision 写 HumanDecision，历史可追溯 |
| 23 | 成绩发布（仅教师） | 已验证 | POST /reviews/{id}/publish 算总分，仅 owner/teacher |
| 24 | 已发布不可改，纠错建新 revision | 已验证 | 发布后 decision 返回 400；纠错走新 review |

## 六、学生侧

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 25 | 查看本人已发布反馈 | 已验证 | GET /submissions/{id}/published；学生仅限本人 |
| 26 | 修订版上传 | 已实现 | 同一学生重复上传自动 version_no 递增；前端上传入口就位 |
| 27 | 版本对比（修订前后内容/证据/评分变化） | 已实现 | GET /submissions/{id}/compare/{v1}/{v2} + 前端对比页 |

## 七、班级分析

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 28 | 班级统计（仅已发布结果） | 已实现 | GET /assignments/{id}/stats（平均/最高/最低/逐项平均）+ 前端统计面板 |

## 八、评估与工程化

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 29 | 评估脚本（三方案对比：准确性/覆盖率/错误放行/成本/耗时） | 已验证 | scripts/evaluate.py 真实跑通：循证评阅 50% > 带证据 25% > 一次性 0% |
| 30 | 自动化测试（单测 + 集成） | 已实现 | 冒烟 14 项 + 解析 3 项 + 评阅/发布/最短路径端到端均通过 |
| 31 | README 与部署文档 | 已实现 | README + docs/deployment.md（架构/步骤/运维/常见问题） |
| 32 | 在线部署 | 已实现 | 前端容器化（Dockerfile+nginx）+ compose 编排 + 部署文档就绪；实际执行需服务器 IP/SSH |
| 33 | 演示材料（3 分钟视频 + PPT） | 待做 | — |

## 九、兼容性验证

| # | 模块 | 状态 | 阻塞 / 依赖 |
| --- | --- | --- | --- |
| 34 | 第二模板（最短路径实验）跑通 | 已验证 | 最短路径端到端 10 项通过（上传→解析→评阅→复核→发布，score=100） |

---

### 当前概览

- 待做：2 ｜ 进行中：0 ｜ 已实现：11 ｜ 已验证：21 ｜ 已发布：0
- **关键路径**：部署文件与文档已全部就绪（前端容器化 + compose 编排 + 部署指南）。剩余待做：#4 demo 工作区、#33 演示材料；实际服务器上线待用户提供 IP/SSH。
